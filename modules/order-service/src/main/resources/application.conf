port = 8080
port = ${?ORDER_SERVICE_PORT}

inventory-service-base-url = "http://localhost:8081"
inventory-service-base-url = ${?INVENTORY_SERVICE_BASE_URL}

postgres {
  host = "localhost"
  host = ${?POSTGRES_HOST}
  port = 5432
  port = ${?POSTGRES_PORT}
  database = "orders"
  database = ${?POSTGRES_DB}
  user = "orders"
  user = ${?POSTGRES_USER}
  password = "orders"
  password = ${?POSTGRES_PASSWORD}
}

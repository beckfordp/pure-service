Phase 3 checkpoint: Typed Error + GET /orders/{id} Route

Automated tests: sbt orderService/test (21/21 passed).

Manual verification (prompting off): scripts/verify-order-reservations.sh runs the
real stack via docker compose + both services, confirms POST /orders -> GET
/orders/{id} round-trips the order with status='reserved' and its reservationId, and
GET /orders/{unknown-id} returns a clean 404 with {"error":"..."}.

Notable finding: the first run failed for real — leftover V1-only rows in the
docker-compose named volume (accumulated across this session's earlier verification
runs) made V2's NOT NULL migration fail, exactly the scenario spec.md's NFR flagged
('assumes a resettable/dev-only database'). Not a code bug; fixed by having the
verify script reset the volume (docker compose down -v) before starting. All checks
passed after that.

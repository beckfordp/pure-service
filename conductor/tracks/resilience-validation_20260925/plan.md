# Plan: Drive a Gatling scenario that ramps inventory-service's induced-failure rate, and add Grafana panels validating resilience effectiveness

## Phase 1: Gatling ramping scenario
- [x] Task: Add a second scenario to `OrderPlacementSimulation.scala` (`rampFailureRate`) that issues `PATCH /admin/induced-failure` requests against a configurable `inventoryBaseUrl` (`-D` property, `localhost:8081` default) on a timed schedule: `0.0` -> `0.6` -> `0.0`, each phase comfortably longer than order-service's 30s `resetTimeout`.
- [x] Task: Wire both scenarios into one `setUp(...)` so they run concurrently; run `sbt loadTest/Gatling/test` against a running `--profile observability` stack and confirm via Prometheus's API that `purerest_circuit_breaker_state` transitions CLOSED -> OPEN -> CLOSED within the run. Verified first run, no calibration needed: 3 CLOSED->OPEN transitions (breaker cycled trip/half-open/retrip twice during the 70s degraded window, each cycle bounded by the 30s resetTimeout), ending back at CLOSED. 304 breaker-rejected calls exactly matched Gatling's 304 KOs (all 503s) and retry's 304 "exhausted" (first-attempt, non-retriable `CircuitBreakerOpen`) outcomes — fully consistent across all three signals.
- [x] Task: Confirm the existing order-placement scenario's own RED metrics still populate normally with both scenarios running concurrently (no interference between them). Verified: 516 `http_server_request_duration_seconds_count` recorded, matching Gatling's ~518 total requests; 211 retry-succeeded outcomes recorded alongside the 304 exhausted — both scenarios' traffic cleanly co-exist.

## Phase 2: Recover and update verify-observability-stack.sh
- [x] Task: Recover `scripts/archive/verify-observability-stack.sh` back to `scripts/` (`git mv`); update README.md/tech-stack.md references back to the un-archived path. `tech-stack.md` no longer contains specific script-path references (dropped in an earlier housekeeping pass) — nothing to change there; README.md's two references and its archived-scripts note updated.
- [x] Task: Replace the script's two-pass structure (healthy pass, then container-restarted degraded pass) with a single run of the updated Gatling simulation; adapt the retry/circuit-breaker assertions to check for a full transition cycle (OPEN and back to CLOSED) rather than just "some activity." Added an explicit check that the live state gauge reports CLOSED at the end of the run (proving full recovery, not just some transition activity).
- [x] Task: Run the updated script end-to-end; confirm all existing assertions (Prometheus targets, Grafana provisioning, Kibana Data View, Elasticsearch log indexing) still pass with the new single-run structure. Verified: all 6 numbered steps (plus 3b) passed, script exited 0, teardown ran cleanly. New checks confirmed: CLOSED->OPEN and back-to-CLOSED transitions both observed, breaker CLOSED at run's end, 304 rejected calls, 39 retried, 3402 indexed log docs including 41 induced-failure WARNs.

## Phase 3: Grafana panels
- [ ] Task: Add "Circuit Breaker: State Timeline" (`state-timeline` panel plotting `purerest_circuit_breaker_state`) to `observability/grafana/provisioning/dashboards/purerest.json`.
- [ ] Task: Add "Retry Success Rate" (`timeseries` panel, successful-including-retried vs exhausted ratio from `purerest_retry_attempts_total`) to the same dashboard.
- [ ] Task: Extend `verify-observability-stack.sh` with assertions that both new panels' PromQL queries (via Grafana's datasource proxy, matching the existing dashboard-query pattern) resolve non-empty data after the ramping run.
- [ ] Task: Run the fully-updated script end-to-end once more; confirm all checks (existing + new) pass.
- [ ] Task: Update tech-stack.md's Load Testing / Local Observability Stack sections to document the new scenario, the recovered script, and the two new panels.
- [ ] Task: Conductor - User Manual Verification 'Phase 3: Grafana panels' (Protocol in workflow.md) — bring up the stack, run the recovered script, open Grafana in a browser, and visually confirm both new panels show a real trip-and-recovery cycle from the run.

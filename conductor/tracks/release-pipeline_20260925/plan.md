# Plan: Add a tag-triggered CI release pipeline that publishes purerest to a real package repository

> **Note (2026-09-25, track `rename-purerestlib_20260925`):** the completed tasks below
> reference the sbt module as `purerest` and the repo as `pure-service` — accurate at the time
> they were run. A subsequent track renamed the sbt module to `purerestlib` and the repo/root
> project to `purerest`; `build.sbt` and `.github/workflows/release.yml` were updated in that
> track to use `purerestlib/publish` and `.../beckfordp/purerest`. Any still-pending task below
> should use the current names.

## Phase 1: Configure GitHub Packages publishing in build.sbt
- [x] Task: Add `publishTo`/`credentials` settings to purerest's `build.sbt` targeting this repo's GitHub Packages Maven registry, reading actor/token from env vars (`GITHUB_ACTOR`/`GITHUB_TOKEN`) so they're unset (and harmless) for local dev. [695dcc3]
- [x] Task: Confirm `sbt purerest/publishLocal` and the existing local dev flow are unaffected by the new settings (env vars unset locally). Verified: `publishLocal` succeeded and published to `~/.ivy2/local` exactly as before — the new `publishTo`/`credentials` settings are inert for that task.
- [x] Task: Conductor - User Manual Verification 'Phase 1: GitHub Packages publishing config' (Protocol in workflow.md). Satisfied by the `publishLocal` check above — a config-only phase with no separate script needed; prompting is off (see `/prompt`), so this substitutes for the interactive walkthrough.

## Phase 2: Tag-triggered release workflow
- [x] Task: Create `.github/workflows/release.yml` — triggers on push of tags matching `v*`; steps: checkout (full history/tags), set up JDK + sbt, run `sbt scalafmtCheck test`, then (only on success) `sbt purerest/publish`, with `permissions: packages: write`. [9360254] (Preceded by [9f42aca]: a whole-repo `scalafmt` reformat — `scalafmtCheck` had never been run repo-wide before, since no CI existed; 11 pre-existing files needed reformatting, unrelated to this track's actual changes, but necessary for the new test gate to pass at all.)
- [x] Task: Confirm the repo's Actions settings allow `GITHUB_TOKEN` to write packages (Settings -> Actions -> General -> Workflow permissions); adjust if needed. Checked via `gh api repos/beckfordp/pure-service/actions/permissions/workflow`: repo default is `read`, but the workflow's own explicit `permissions: packages: write` block overrides the repo default for that workflow — no settings change needed.
- [x] Task: Push a real test tag (e.g. `v0.0.1-ci-test`) — requires explicit user go-ahead, since this pushes to the shared remote and triggers a real CI run — confirm the workflow runs end-to-end and the package appears on the repo's GitHub Packages page. Clean up the test tag/package afterward. Verified 2026-09-25: an initial test tag (`v0.0.1-ci-test`) hit an account-level GitHub Actions billing lock (unrelated to this track's config); after the user resolved billing, a fresh tag `v0.0.2-ci-test` was pushed from current `main` and the workflow ran end-to-end — `scalafmtCheck test` and `purerestlib/publish` both succeeded, publishing `io.github.beckfordp.purerestlib_3:0.0.2-ci-test` to GitHub Packages (confirmed via API). The test tag was deleted from the remote afterward; the test package version was left in place at the user's request (GitHub disallows deleting a package's only version without deleting the whole package).
- [x] Task: Confirm the test gate actually blocks publishing on failure (temporarily break a test on a disposable branch/tag, confirm the job stops before the publish step, then revert/delete). Verified 2026-09-25: pushed a disposable branch (`test-gate-failure-check`) and tag (`v0.0.3-ci-test-fail`) containing a deliberately failing test (`GateFailureCheckSuite`); the workflow's `Test gate` step failed and `Publish purerest` was skipped (never ran) — no package was published. Branch and tag deleted from the remote afterward; nothing merged to `main`.
- [ ] Task: Conductor - User Manual Verification 'Phase 2: Tag-triggered release workflow' (Protocol in workflow.md).

## Phase 3: External resolution + documentation
- [x] Task: Point `smoke-test/`'s standalone build at GitHub Packages (documented flag/profile) and confirm it resolves and runs against the tag-published version, not just the local Ivy2 cache. Added an opt-in `-DresolveFromGitHubPackages=true` flag to `smoke-test/build.sbt` (documented inline) that adds the GitHub Packages resolver + credentials (`GITHUB_ACTOR`/`GITHUB_TOKEN`), mirroring the root build's publish-side credentials pattern; default behavior (local Ivy2) is unchanged when the flag is unset. Verified 2026-09-25: ran `sbt -DpurerestVersion=0.0.2-ci-test -DresolveFromGitHubPackages=true test` against the CI-published test version — 1 test passed. Confirmed genuine external resolution, not a cache false-positive: `~/.ivy2/local` has no `purerestlib_3` entry at all (that version was never `publishLocal`'d on this machine), and the resolved jar/pom were found cached under `~/Library/Caches/Coursier/v1/https/maven.pkg.github.com/beckfordp/purerest/...` — the GitHub Packages URL itself.
- [ ] Task: Update `tech-stack.md`'s Publishing section (remove "No CI/release automation yet", document the pipeline/tag convention/GH Packages resolution) and README.md (how a consumer adds the resolver + PAT).
- [ ] Task: Conductor - User Manual Verification 'Phase 3: External resolution + documentation' (Protocol in workflow.md).

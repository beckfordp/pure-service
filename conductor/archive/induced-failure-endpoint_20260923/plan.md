# Plan: Add a runtime-adjustable induced-failure endpoint to inventory-service

## Phase 1: Runtime-Adjustable Induced-Failure Config [checkpoint: da61575]
- [x] Task: Write failing tests (Red) for `GET /admin/induced-failure` returning the current config, backed by a `Ref[F, InducedFailureConfig]` seeded with a known value.
- [x] Task: Change `InventoryRoutes` to hold induced-failure config in a `Ref[F, InducedFailureConfig]` instead of a plain value (`serverEndpoint`/`routes` now take the Ref; `/inventory/reserve` reads it fresh per request); add the `GET /admin/induced-failure` tapir endpoint. Update existing tests to construct via `Ref.of(...)`. Implement to pass (Green).
- [x] Task: Write failing tests (Red) for `PATCH /admin/induced-failure` with a valid body updating the Ref, and a subsequent `GET` reflecting the new values.
- [x] Task: Implement the `PATCH` endpoint (Green).
- [x] Task: Write failing tests (Red) asserting `PATCH` rejects `failureRate` outside `[0.0, 1.0]` and negative `delayMs` with 400 + a typed JSON error body, leaving the live config unchanged.
- [x] Task: Model the validation error as a typed domain error (per `product-guidelines.md`) and wire it to a 400 response. Implement to pass (Green).
- [x] Task: Write a failing integration-style test (Red): GET baseline -> PATCH `failureRate=1.0` -> `POST /inventory/reserve` fails (500) -> PATCH `failureRate=0.0` -> `POST /inventory/reserve` succeeds — proving the change takes effect live without a restart.
- [x] Task: Make this test pass (Green), fixing anything needed for the live read-through to work end-to-end. No further changes needed — passed immediately, confirming `configRef.get`'s per-request read (added in task 2) already made the whole flow live.
- [x] Task: Wire `Main.scala` to construct the Ref at startup (seeded from the existing `INVENTORY_INDUCED_FAILURE_RATE`/`_DELAY_MS` env vars, unchanged default behavior) and register both new endpoints alongside `/inventory/reserve` in `Docs.routes`.
- [x] Task: Run `sbt coverage inventoryService/test coverageReport`; confirm high coverage on changed files and the full suite green. Verified: InventoryRoutes.scala 100%/100% (line/branch); full suite 19/19 green. Module-wide aggregate (71%/86%) reflects Main.scala's untested side-effecting entrypoint, per existing project convention.
- [x] Task: Refactor `maybeInduceFailure`/`serverEndpoint` to stop using an exception for control flow (violates `product-guidelines.md`'s "no exceptions for control flow" rule, caught in review): `maybeInduceFailure` returns `F[Either[ReserveError, Unit]]` instead of raising; renamed `serverEndpoint` to `reserveServerEndpoint` (matches the `getInducedFailureServerEndpoint`/`patchInducedFailureServerEndpoint` naming, avoiding the collision with the existing private `reserveEndpoint` tapir description); gave `reserveEndpoint` a real error channel (`ReserveError`/`InducedFailureTriggered` -> 500, same `errorOut` shape as `OrderNotFound`/`InvalidInducedFailureConfig`) and switched to `.serverLogic`. Updated all call sites (`Main.scala`, `InventoryDocsSuite.scala`). All 19 existing tests pass unchanged (500 status, WARN log content preserved). Deviation from plan: the 500 response now carries a small `{"error": "Induced failure"}` JSON body (previously bodyless) — the bodyless `errorOut` left one line of the encode direction structurally uncoverable (tapir never calls it for a bare fixed-status output with no dynamic content to write), so a real body was added instead, both fixing coverage and matching the other two typed errors' pattern in this file. Verified: `InventoryRoutes.scala` 100%/100% (line/branch); inventoryService 19/19, orderService 28/28 green.
- [x] Task: Conductor - User Manual Verification 'Phase 1: Runtime-Adjustable Induced-Failure Config' (Protocol in workflow.md) — started inventory-service via `sbt inventoryService/bgRun`; ran the full GET/PATCH/GET/POST-reserve/PATCH-invalid sequence against the live server, confirming the failure rate takes effect live (baseline 201 -> PATCH 1.0 -> 500 with a typed error body -> PATCH 0.0 -> 201 again, no restart), invalid PATCH input is rejected 400 without mutating state, and both `get`/`patch` on `/admin/induced-failure` appear in the generated `docs.yaml`/Swagger UI (`/docs`, 200 after redirect). User confirmed the results meet expectations.

## Phase: Review Fixes
- [x] Task: Apply review suggestions [749a51b]

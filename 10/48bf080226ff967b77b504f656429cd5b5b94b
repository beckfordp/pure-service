Phase Completion Report: Phase 1 - Enrich Logging

Automated verification: sbt coverage purerest/test orderService/test
inventoryService/test coverageReport
- purerest 44/44, inventoryService 12/12, orderService 26/26, all green.
- 100% statement/branch coverage on OrderRoutes.scala and
  InventoryRoutes.scala (the files this phase changed with testable logic).
- Main.scala files at their established 0% (side-effecting entrypoints,
  never unit-tested in this project).

Manual verification: no running observability stack to demo against yet
(that's Phases 3-4), so this substitutes the usual interactive HTTP
walkthrough with: `sbt inventoryService/bgRun`, then `curl localhost:8081`
to trigger readiness, confirming "inventory-service starting" with
port/metrics_port/induced_failure_rate context actually appears in console
output. Confirmed present.

Result: All checks passed. Phase 1 complete.
